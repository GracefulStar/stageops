# Local Docker Compose defaults only. Use a different secret for public deployment.
STAGEOPS_DB_PASSWORD=stageops-local-demo
STAGEOPS_ALLOWED_ORIGINS=["http://localhost:8000"]
STAGEOPS_COOKIE_SECURE=false
